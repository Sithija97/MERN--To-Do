export enum BaseTypes {
  NOTE = "note",
  CATEGORY = "category",
}

export enum AddNoteModalType {
  NEW = "new",
  EDIT = "edit",
}

export enum NoteStatus {
  IDLE = "idle",
  HIGH = "high",
  MEDIUM = "medium",
  LOW = "low",
}
